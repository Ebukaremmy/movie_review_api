from django.apps import AppConfig

class ReviewsConfig(AppConfig):
    """
    Application configuration for the Reviews module.
    Handles the registration and initialization of the reviews system within the project.
    """
    name = 'reviews'